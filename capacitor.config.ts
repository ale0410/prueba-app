import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.app',
  appName: 'PruebaApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
